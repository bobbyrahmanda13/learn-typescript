const User = {
  name: "rahman",
  email: "rahman@gmail.com",
  isActive: true,
}
function createUser({ name: string, isPaid: boolean }) {}

createUser({ name: "rahman", isPaid: false })

function createCourse(): { name: string; price: number } {
  return {}
}

export {}
